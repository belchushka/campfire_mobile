import { StyleSheet } from "react-native";

export default StyleSheet.create({
  button: {
    paddingVertical: 16,
    borderRadius: 16,
    height: 54,
    justifyContent: "center",
    alignItems: "center",
  },
  button_text: {
    fontSize: 18,
  },
});
