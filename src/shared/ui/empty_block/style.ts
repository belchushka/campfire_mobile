import { StyleSheet } from "react-native";
import { colors } from "styles/index";

export default StyleSheet.create({
  body: {
    alignItems: "center",
    alignSelf: "center",
  },
  text: {
    color: colors.grey,
    fontSize: 20,
  },
});
