import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  text: {
    color: "black",
    fontSize: 30,
  },
  wrapper: {
    flexGrow: 1,
    flexDirection: "row",
    alignItems: "center",
  },
});

export default styles;
